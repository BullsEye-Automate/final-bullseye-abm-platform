# Peitho — Contexto y progreso (backend)

> Este archivo lo lee Claude Code automáticamente al trabajar dentro de `peitho-backend/`.
> Actualízalo cada vez que se completa o se decide algo relevante, para no repetir contexto en sesiones futuras.

---

## Qué es Peitho

Producto de *conversation intelligence* para reuniones de venta B2B, construido por Bullseye-ABM. Dos casos de uso:

1. **Uso interno de Bullseye:** captura y analiza automáticamente las llamadas comerciales de sus propios SDRs (reemplaza DIIO).
2. **Producto para clientes de Bullseye** (ej. CCHC): genera un brief de preparación antes de una reunión que Bullseye le agendó a un prospecto del cliente.

Restricción de diseño clave: **no debe aparecer un bot visible en la llamada** — la captura de audio es vía extensión de Chrome (`chrome.tabCapture`) en el navegador del ejecutivo, no un participante que se une a la reunión.

Documentos de referencia completos en `docs/`:
- `docs/PEITHO_BRIEF_claude_code.md` — brief general, stack, las 8 tareas en orden
- `docs/peitho_arquitectura_tecnica_v1.md` — diseño de los 2 flujos (post-reunión y pre-reunión) y tabla `meetings`
- `docs/peitho_prompt_analisis_v1.md` — prompt de análisis post-reunión (ya calibrado con minutas reales)
- `docs/peitho_prompt_pre_reunion_v1.md` — prompt de brief pre-reunión

---

## Stack

- Node.js v20 + TypeScript, Express
- Postgres vía Supabase (proyecto "peitho" del usuario), acceso con `pg` (pool de conexión)
- Pendiente de integrar más adelante: Deepgram (STT + diarización), Anthropic API (Claude, para correr los prompts), Google Calendar API, Slack webhook, Railway/Render (deploy)

---

## Progreso de tareas (ver orden completo en `docs/PEITHO_BRIEF_claude_code.md`)

- [x] **Tarea 1** — Esqueleto backend + tabla `meetings` + `GET /health`. Completado y probado localmente contra Supabase (confirmado por el usuario: `{"status":"ok","db":"connected"}`).
- [x] **Tarea 2** — OAuth de Google Calendar + webhook `events.watch` → guarda eventos nuevos en `meetings`. Completada y confirmada por el usuario end-to-end: creó un evento de prueba con Meet + invitado en Google Calendar, y apareció en `meetings` con `meet_code`, `ejecutivo` y `contraparte` correctos.
- [x] **Tarea 3** — `GET /meetings/lookup?meet_code=xxx`. Completada y confirmada: responde `{"registered":true,...}` para un meet_code existente y `{"registered":false}` para uno inexistente.
- [x] **Tarea 4** — Extensión de Chrome (Manifest V3, `chrome.tabCapture`) + `POST /meetings/:id/audio`. Completada y confirmada end-to-end: detección automática (ícono celeste), clic del usuario para autorizar la captura (ícono rojo), subida del audio al cerrar la pestaña, y la fila en `meetings` pasó a `status=captured` con `audio_path` lleno.
- [x] **Tarea 5** — Pipeline de análisis post-reunión (Deepgram + prompt de `peitho_prompt_analisis_v1.md` vía Anthropic API → `meetings.analysis`). Completada y confirmada end-to-end: audio real (grabado desde un segundo dispositivo hablando en la llamada) → transcripción con Deepgram → análisis con Claude → `meetings.analysis` con el JSON completo y `status=analyzed`. **Fix de audio (mic + reconexión a parlantes) confirmado end-to-end** con una llamada real de dos personas (Jaime): el ejecutivo escuchó con normalidad durante toda la grabación (el bug original quedó resuelto). Tarea 5 dada por cerrada por el usuario.
- [x] ~~**Tarea 6** — Notificación de feedback listo (Slack o correo).~~ **Reemplazada** por decisión explícita del usuario: en vez de una notificación simple, Peitho va a tener un frontend propio con 2 módulos (ver "Roadmap frontend" abajo). No se construye la notificación Slack/correo — el análisis se ve directo en la app.
- [ ] **Tarea 7 (brief original)** — absorbida dentro del roadmap del frontend (Módulo 1), ampliada con research de LinkedIn/señales comerciales (más allá de la búsqueda web básica que describía el MVP original).
- [ ] **Tarea 8 (brief original)** — absorbida dentro del roadmap del frontend (Módulo 1: página de detalle de reunión futura, no una página de brief aislada).
- [ ] **(Al final)** Deploy a Railway o Render (backend) y Vercel (frontend) — el usuario decidió explícitamente dejarlo para el final; hasta entonces se prueba todo local con `npm run dev`.

**Regla de trabajo del usuario:** una tarea a la vez, probarla él mismo antes de avanzar a la siguiente. No adelantarse a tareas futuras sin que lo pida.

---

## Roadmap frontend (decidido — reemplaza Tareas 6-8 del brief original)

Después de confirmar la Tarea 5, el usuario pidió un cambio de rumbo: en vez de una notificación simple, Peitho va a tener su propio frontend con 2 módulos, similar a DIIO:

1. **Módulo 1 — Preparación de reuniones futuras:** listado de reuniones agendadas; al entrar a una, mostrar toda la info de la empresa/prospecto (research en internet + LinkedIn: señales, rompehielos, experiencias — todo lo que sirva para la propuesta de valor de BullsEye). Amplía el Flujo 2 (`peitho_prompt_pre_reunion_v1.md`), que en su versión MVP original solo contemplaba "búsqueda web básica" y explícitamente dejaba fuera de scope el research de LinkedIn — eso ahora sí entra en scope.
2. **Módulo 2 — Análisis de reuniones pasadas:** tipo DIIO, con el análisis ya generado en la Tarea 5 (`meetings.analysis`), agregando un **puntaje de desempeño del vendedor de 1 a 10** con oportunidades de mejora claras (hoy el prompt de análisis solo tiene sub-métricas 1-5 por categoría en `metricas_desempeno_ejecutivo`, no un puntaje global 1-10 — hay que agregarlo al esquema/prompt).

**Decisiones de arquitectura tomadas:**
- El frontend va en una **app nueva y separada** (ej. `peitho-frontend/`), no dentro de `bullseye-abm-platform` (la app Next.js principal) — consume la API de `peitho-backend`. Consistente con que Peitho ya está diseñado como producto independiente (proyecto Supabase propio, `peitho`, distinto al de BullsEye ABM).
- Se descartó explícitamente hacer la notificación Slack/correo de la Tarea 6 original como paso intermedio — se va directo al frontend.
- El usuario quiere pasar el diseño visual por "Claude Design" antes de construir las UIs — pedirá ayuda para redactar ese prompt cuando llegue el momento (no bloquea empezar el esqueleto del frontend).

**Desglose de tareas** (confirmado — "como tú estimes conveniente" del usuario, delegó el orden):
- [x] **Paso 1 — Esqueleto del frontend.** `peitho-frontend/` (Next.js 14 App Router + Tailwind + Supabase Auth, mismo patrón que `bullseye-abm-platform/middleware.ts`) con login y dos páginas (`/reuniones/futuras`, `/reuniones/pasadas`) que listan datos reales desde `GET /meetings?scope=upcoming|past` en `peitho-backend`. **Confirmado end-to-end por el usuario** (login real contra el proyecto Supabase "peitho", datos reales en ambas listas). Incluye además, agregado durante la misma prueba:
  - Página de detalle compartida `/reuniones/[id]` (no separada por módulo — sirve tanto para una reunión futura como pasada) con `GET /meetings/:id` en el backend, mostrando el `analysis` completo (predicción de éxito, resumen, métricas de desempeño 1-5, objeciones, dolores del cliente, compromisos, temas pendientes, recomendaciones) o un mensaje si todavía no hay análisis. **Confirmado funcionando** con datos reales.
  - `GET /meetings?scope=past` acotado a los últimos 90 días (había cientos de ocurrencias históricas desde 2020 de una reunión recurrente, sincronizadas antes del fix de timeMin/timeMax de la Tarea 2).
  - Ambos scopes excluyen reuniones internas (contraparte con el mismo dominio `bullseye-abm.com` que BullsEye) — Peitho solo debe mostrar reuniones con prospectos reales. **Confirmado por el usuario:** ya no aparecen las internas.
- [x] **Paso 2 y 3 — Research manual de empresa/prospecto.** `POST /meetings/:id/research` (backend) + botón "Iniciar research" en `/reuniones/[id]` (frontend), implementado según el requisito explícito del usuario: **no corre automático** — cada reunión tiene su propio botón, para no gastar créditos de API en reuniones que no son con un prospecto real. Migración `006_add_pre_brief_to_meetings.sql` (`pre_brief jsonb`, `pre_brief_status`). El research usa la herramienta nativa `web_search` de Claude (`claude-sonnet-5`, tipo `web_search_20260209`, `max_uses: 5`) para que el propio modelo busque la empresa/contacto — no se integró ningún servicio externo nuevo (ni LinkedIn API ni scraping dedicado; es "lo que aparezca en una búsqueda web pública", igual que el MVP original, ahora con más señales gracias al tool use). Sección "Investigación de empresa y prospecto" en la página de detalle, con polling simple mientras corre. **Confirmado funcionando end-to-end por el usuario** tras tres rondas de fixes reales (timeout muy corto, calidad de búsqueda pobre, y estado "running" que quedaba colgado — ver "Decisiones y aprendizajes"): probó con una reunión real (`bruno@samu.ai` / Samu.ai) y el brief final salió con señales concretas y bien accionables (ronda de financiamiento, expansión a México/Colombia, nombre del fundador, análisis del rol de decisión).
- [x] **Paso 4 — Puntaje 1-10 de desempeño del vendedor.** Nuevo campo `desempeno_vendedor` en el esquema de análisis (`puntaje` 1-10, `resumen`, `oportunidades_mejora[]`) — ver `docs/peitho_prompt_analisis_v1.md` sección 3.2. Distinto de `prediccion_exito` (que mide probabilidad de cierre del deal, no habilidad del vendedor) y de `metricas_desempeno_ejecutivo` (5 sub-métricas 1-5 ya existentes — el nuevo puntaje es un juicio holístico basado en ellas, no un promedio mecánico). Ya renderizado en `/reuniones/[id]`. **Pendiente de que el usuario lo pruebe** con una reunión nueva (las ya analizadas antes de este cambio no tienen este campo — no hay mecanismo de re-análisis retroactivo).
- [x] ~~**Paso 5** — UI del Módulo 2 (página de detalle tipo DIIO con el análisis y el puntaje).~~ Completo — la página de detalle muestra el análisis completo incluyendo el puntaje 1-10 del Paso 4.

**Research ampliado + Módulo 3 (Base de conocimiento) + multi-cliente** (pedido nuevo del usuario, dividido en fases — "una a la vez"):
- [x] **Fase A — Enriquecer el research con el excel de metas.** Nueva tabla `clients` + columnas `contacto_nombre/cargo/industria` y `client_id` en `meetings` (migración `008_add_clients_and_contact_enrichment.sql`). `metasSheet.ts` lee el spreadsheet de Google Sheets **"Metas equipo BullsEye / reuniones / oportunidades"** (tab **"API Reuniones - IA"**, auto-detectado por sus columnas — no hardcodeado por si se renombra), matchea cada reunión por empresa + fecha, y resuelve/crea el `client` correspondiente. Es gratis (no llama a Claude) y corre automático al cargar el detalle de la reunión, no solo al hacer clic en "Iniciar research". El prompt de research (`preReunion.ts`) usa estos datos como ciertos en vez de adivinarlos. **Confirmado end-to-end por el usuario** (reunión real: contacto "Felipe Almazan", cargo "Gerente de Cuentas", industria "Tecnologia", cliente "BullsEye" — sí, BullsEye aparece como un cliente más dentro del mismo excel, agenda reuniones para sí misma además de para sus clientes).
- [x] **Fase B — LinkedIn ampliado.** Con el nombre/cargo ya confirmados por la Fase A, se amplió el research con tres campos nuevos: `experiencia_contacto`, `icebreakers_sugeridos` y `competidores_directos` (esquema y prompt en `preReunion.ts`). `max_uses` de `web_search` subido de 5 a 8, timeout de 3 a 4 min. También se agregó la opción de pegar a mano la URL de LinkedIn del contacto (migración `009_add_contacto_linkedin_url.sql`, `PUT /meetings/:id/contacto-linkedin`, componente `LinkedinUrlForm`) para los casos con homónimos (ej. "Felipe Almazan"). Se probó `web_fetch` para traer esa URL directo, pero **LinkedIn la bloquea a nivel de política de la herramienta** (`web_fetch_tool_result_error` / `url_not_allowed`, confirmado real) — se sacó `web_fetch` y la URL ahora solo se usa como pista de texto para `web_search` (el slug de la URL a veces trae apellidos que el excel de metas no tenía). **Confirmado end-to-end por el usuario**: con la reunión de Felipe Almazan, el research encontró correctamente los competidores reales (Computrabajo, Laborum, Chiletrabajos, Bumeran) y dejó `experiencia_contacto`/`icebreakers_sugeridos` vacíos en vez de inventar, tal como se pidió.
- [x] **Fase C — Módulo 3: Base de conocimiento por cliente.** Nueva sección "Base de conocimiento" (`/base-de-conocimiento`) — listado de clientes (con alta manual además de la creación automática vía `metasSheet.ts`) y, por cliente, subir/listar/borrar documentos. Migración `010_add_knowledge_base.sql` (`knowledge_base_documents`: client_id, file_name, storage_path, content extraído). Los archivos se suben a **Supabase Storage** (bucket `knowledge-base`, nunca al disco del backend), y el texto se extrae con `officeparser` (cubre pdf/docx/pptx/xlsx/odt/odp/ods/rtf con una sola librería detectando el formato por los bytes). Todavía **no se usa en ningún prompt** — eso es la Fase D. **Confirmado end-to-end por el usuario** tras una ronda larga de fixes reales (ver "Decisiones y aprendizajes"): subió el ICP completo de BullsEye en PDF y quedó listado correctamente.
- [ ] **Fase D — Conectar la Base de conocimiento a research y análisis post-reunión.** Nuevo `getClientKnowledgeBaseContext()` en `knowledgeBase.ts` (concatena el contenido extraído de los documentos del cliente, tope de 30k caracteres) reutilizado por ambos flujos:
  - **Research pre-reunión:** nuevos campos `temas_recomendados`, `temas_evitar` y `casos_exito_sugeridos` en `preReunion.ts`, grounded en la base de conocimiento del cliente resuelto para la reunión. **Confirmado end-to-end por el usuario** (reunión real de Trabajando.com/Felipe Almazán): el brief citó explícitamente el ICP de BullsEye para justificar los temas recomendados/a evitar, y dejó `casos_exito_sugeridos` vacío porque el ICP subido no trae casos de éxito nombrados — correcto, no inventó nada.
  - **Análisis post-reunión:** se reemplazó el `EMPRESA_CLIENTE = 'BullsEye'` hardcodeado por el cliente real resuelto de la reunión (con `resolveMeetingClientAndContact`, mismo patrón que el research — "BullsEye" queda solo como fallback), y se agregó instrucción para evaluar `propuesta_valor_presentada`/`recomendaciones_proximos_pasos` contra la base de conocimiento real. **Pendiente de probar** — el usuario no tenía reuniones próximas para grabar en el momento de este cambio; no hay forma de forzar el re-análisis de una reunión ya analizada, así que queda pendiente hasta la próxima reunión real que se grabe con audio.
- [x] **Fase E — Login por cliente + vista admin.** Cada cliente de BullsEye tiene su propio login y ve **todo** el detalle (feedback, research, análisis completo) de **sus propias** reuniones — el usuario decidió explícitamente que no hay restricción de qué campos ve (ni el puntaje del vendedor ni el coaching interno quedan ocultos). Admin ve y filtra todos los clientes. **Confirmado end-to-end por el usuario**: probó con un usuario de prueba (rol "cliente" → BullsEye) en una ventana de incógnito — sidebar sin "Administración", reuniones futuras/pasadas ya scopeadas a BullsEye sin selector de cliente, detalle de reunión completo (ficha del contacto + investigación) pero sin el botón "Iniciar research" ni el campo de LinkedIn, y base de conocimiento redirigiendo directo a la de BullsEye sin formulario de subida ni botón de borrar.
  - Migración `011_add_peitho_user_roles.sql` — tabla `peitho_user_roles` (`user_id` de Supabase Auth, `email`, `role` admin|client, `client_id`).
  - `src/authMiddleware.ts` — `requireAuth` valida el JWT de Supabase contra el propio servidor de Supabase (`supabase.auth.getUser(token)`, vía el cliente admin — service_role) y busca el rol en `peitho_user_roles`; `requireAdmin` exige rol admin. **Esto es lo que hace el scoping real a nivel de API**, no solo gating de frontend — un usuario "client" que llame al backend directo con su propio token nunca puede ver datos de otro cliente.
  - Aplicado a `GET/POST /clients`, `GET/POST/DELETE /clients/:id/documents`, `GET /meetings`, `GET /meetings/:id`, `PUT /meetings/:id/contacto-linkedin`, `POST /meetings/:id/research`. **NO aplicado** a `GET /meetings/lookup` ni `POST /meetings/:id/audio` (los llama la extensión de Chrome, sin sesión de Supabase) ni a los webhooks/OAuth de Google Calendar.
  - Decisiones de alcance sobre qué puede hacer un rol "client" (no estaban explícitas en el pedido original, se decidieron por default conservador): puede ver el detalle completo de sus reuniones y su base de conocimiento (ver/listar), pero **no** puede subir/borrar documentos, iniciar research, ni pegar la URL de LinkedIn de un contacto — esas acciones quedan admin-only (`requireAdmin`) porque son acciones operativas de BullsEye, no de consumo del cliente.
  - `GET /clients` es admin-only (lista completa) — un "client" nunca la necesita, va directo a `/clients/:id/documents` de su propio `client_id` (scoping validado en el handler, 404 si no coincide).
  - `GET /meetings` acepta `?client_id=` para el filtro del admin; para rol "client" se ignora cualquier `client_id` que mande y se fuerza el suyo.
  - Nuevo `GET /me` (rol + client_id + nombre del cliente) y `GET/POST /admin/user-roles` + `DELETE /admin/user-roles/:userId` (admin-only) para asignar/revocar acceso desde la UI (`/admin/usuarios`) en vez de SQL a mano — busca el usuario por email en Supabase Auth (tiene que existir de antes, se crea en Supabase Studio, mismo patrón de "no hay signup público") y le upsertea un rol.
  - Frontend: `/reuniones/futuras` y `/reuniones/pasadas` muestran un selector de cliente (solo admin) y una columna "Cliente" en la tabla; `/base-de-conocimiento` redirige a un "client" directo a la base de su propio cliente (sin ver el listado completo); `/base-de-conocimiento/[id]` oculta el formulario de subida y los botones de borrar para "client"; `/reuniones/[id]` oculta el botón "Iniciar research" y el formulario de LinkedIn para "client"; Sidebar muestra "Administración" solo para admin.
  - **Primer admin:** como el propio `/admin/usuarios` requiere ser admin para entrar, el primer rol admin se inserta a mano por SQL en Supabase Studio (ver sección "Cómo correr y probar localmente" más abajo).
- [ ] **Fase F — Categorías en la Base de conocimiento + "Generar PPT personalizada".** Confirmado que se va a hacer ("si o si"), no es idea de backlog:
  - **Categorizar la Base de conocimiento (Módulo 3) por tipo de documento.** Hoy todos los documentos de un cliente viven en una sola lista sin categorizar (`/base-de-conocimiento/[id]`). El usuario pidió (con una captura de referencia — sidebar con secciones "Empresa" → Todo el material / Propuesta de valor / ICP y perfiles, "Ventas" → Presentaciones / Casos de éxito / Manejo de objeciones, "Multimedia" → Videos comerciales / Imágenes-Logos, cada una con contador de documentos) reorganizar la sección en categorías así, en vez de la lista plana actual. Implica agregar una columna/tabla de categoría a `knowledge_base_documents` (o una tabla de categorías fija) y rehacer la UI de `/base-de-conocimiento/[id]` como un sidebar de categorías en vez de una lista única.
  - **"Generar PPT personalizada" en el flujo de preparar la reunión.** Nueva opción dentro de "Iniciar research"/preparación de la reunión: generar con IA una versión adaptada de la PPT comercial estándar del cliente para esa reunión específica — no reescribirla completa, sino personalizar ciertas partes (casos de éxito relevantes al prospecto, mención de la empresa contraparte y del contacto, etc.), usando el mismo research que ya se genera para el brief pre-reunión. Requiere como prerrequisito que el cliente tenga su PPT comercial estándar subida en la Base de conocimiento (categoría "Presentaciones" de la idea anterior). Implica manipulación de archivos .pptx con IA (revisar librerías de edición de pptx en Node, o generar de cero con una librería y no editar el archivo original) — evaluar alcance técnico antes de comprometerse a una fecha.
- [ ] **Fase G — Refresh de diseño de Peitho.** Rediseñar la UI de `peitho-frontend` (la app operativa, no la landing de marketing que el usuario ya está armando aparte con Claude Design) apoyándose en Claude Design con prompts redactados a medida — mismo enfoque que `docs/prompt_logo_peitho.md` para el logo. Pendiente de definir alcance exacto (¿todas las pantallas o solo las más visibles primero?) cuando se llegue a esta fase.
- [ ] **Fase H — Bot de Recall.ai reemplaza la extensión de Chrome.** El usuario quiere que Peitho funcione con un bot que se una a la reunión y la grabe (como DIIO/Gong), no que dependa de que el ejecutivo autorice una extensión manualmente. Decidido usar **Recall.ai** (aprobado en su programa de startups: **$0.25/hora las primeras 10.000 horas**, aplicado ya a la cuenta "BullsEye" en la región `us-west-2`). El bot solo reemplaza *cómo* llega el audio — el resto del pipeline (Deepgram → Claude, `analyzeMeetingAudio` en `postMeetingAnalysis.ts`) no cambia.
  - **Por qué Recall y no construirlo:** se evaluó extender la extensión de Chrome a un bot con Playwright (visto en Fase D/"ideas v2"), pero se descartó mantener nosotros mismos la lógica de unirse/grabar en cada plataforma (Zoom/Meet/Teams) y el riesgo de que la plataforma banee al bot. Se evaluaron también MeetingBaaS y Vexa (open-source) como alternativas — se eligió Recall por mejor cobertura/confiabilidad y porque **ya tiene una integración nativa con Deepgram** (el mismo proveedor que ya usamos), lo que da transcripción con el nombre real de cada hablante en vez de la heurística actual ("quien habla primero es el ejecutivo").
  - **Costo real estimado:** a volumen actual (~170 reuniones/mes × 1h) el costo de grabación es ~US$85/mes (a precio normal; con el descuento del programa de startups, una fracción de eso durante las primeras 10.000 horas). La transcripción no es un costo nuevo — ya se paga a Deepgram hoy, solo cambia quién la dispara. **No hace falta pagarle a Recall por guardar nada a largo plazo:** apenas el webhook avisa que la grabación está lista, el backend baja la transcripción/audio y la guarda en la propia base de Peitho — Recall solo cobra por grabar/transcribir, no por retención (7 días gratis, que nunca se llegan a usar si se procesa al toque).
  - **Bot autenticado, no anónimo:** para que el bot aparezca con nombre "Peitho" (no genérico) y pueda saltarse la sala de espera, tiene que ser un **bot "signed-in"** (con una cuenta de Google real detrás), no anónimo.
  - **Descubrimiento importante (evitó un problema serio):** la documentación de Recall exige que la cuenta de Google del bot viva en un **Workspace de Google nuevo y dedicado**, nunca en el Workspace real de la empresa — porque el método de login se basa en una política de SSO a nivel de organización que "romperá el login de todas las cuentas del Workspace existente" si se configura ahí. Por esto se descartó usar `bullseye-abm.com` y se compró un **dominio nuevo, `peithob2b.com`** (en GoDaddy, sin necesidad de página web ni empresa constituida — Google Workspace es autoservicio, solo pide poder verificar el dominio vía DNS), con su propio Workspace de pago (plan Starter).
  - **Setup ya hecho, paso a paso:**
    1. Cuenta admin del Workspace nuevo (José Antonio Karmy) + cuenta separada **no-admin** para el bot: `bot@peithob2b.com`, nombre de perfil "Peitho" (el nombre que se ve en la reunión sale del *nombre de la cuenta de Google*, no del parámetro `bot_name` de la API, ni del email).
    2. En Recall Dashboard (Platform → API Explorer → Google Login): se creó el **Login Group** "Peitho" (`id: 184075cb-94fb-42d5-a12a-66ceb1d20bad`) y dentro un **Google Login** con `bot@peithob2b.com` — usando un par llave privada/certificado autofirmado (`openssl req -x509 -newkey rsa:2048 ... -days 3650 -nodes`), no una contraseña.
    3. En el Admin console de `peithob2b.com` (no el de BullsEye): Security → Authentication → SSO with third party IdP → Add SAML profile → al fondo de esa página, "Go to legacy SSO profile settings" (Google fusionó esta pantalla vieja dentro de la UI nueva de perfiles SAML/OIDC) → se habilitó, se configuraron las URLs de sign-in/sign-out de Recall (`https://us-west-2.recall.ai/api/v1/bot/gmeet-sign-in` / `.../gmeet-sign-out`, específicas de la región `us-west-2` de la cuenta), se subió el certificado, y se marcó "Use a domain-specific issuer". El perfil quedó asignado automáticamente al org unit por defecto.
    4. Confirmado con una llamada real a la API (`POST https://us-west-2.recall.ai/api/v1/bot/`) que el bot se crea correctamente — **de paso confirmó que el endpoint de bot es `/api/v1/bot/`, no `/v2/` (v2 da 404)**, y que el campo `metadata` sí existe en el schema (útil para poder pasar de vuelta nuestro `meetingId` — ver `recall.ts`).
  - **Resuelto — `sso_not_configured` persistente (no era propagación de DNS/SSO).** El bot falló con `"Your domain is not configured to use Single Sign On"` (`sub_code: sso_not_configured`) de forma consistente durante 48+ horas, mucho más allá de la ventana de propagación de 24h documentada por Google — y con toda la configuración de SSO (perfil legacy habilitado, URLs exactas, certificado subido, "domain-specific issuer" marcado) verificada correcta campo por campo. Se descartó en el camino que `bot@peithob2b.com` fuera Super Admin del Workspace (Google Workspace nunca aplica SSO de terceros a cuentas Super Admin, por diseño) — la cuenta del bot no tiene ningún rol de administrador. **Causa real:** el Workspace `peithob2b.com` (creado "hace 2 días") seguía en **período de prueba gratis de Business Starter, sin convertir a suscripción de pago** (`Admin console → Facturación` mostraba "El servicio de pago comienza en 11 días"). Ciertas funciones de seguridad — SSO con IdP de terceros incluido — no quedan completamente activas hasta que la suscripción pasa a estar en curso de pago real, aunque el panel ya muestre el perfil SSO como "Habilitada". **Fix:** convertir la suscripción a pago desde ese mismo panel ("Inicio del servicio pagado"). Confirmado funcionando minutos después — bot "Peitho" entró a una reunión de prueba real y quedó en estado `in_call_recording` (`bot_id: 2300017d-7b5a-4e35-aa39-464adee4267`). **Aprendizaje para el futuro:** si se repite este bloqueo en otro dominio/cliente, revisar primero el estado de facturación del Workspace antes que la configuración de SSO en sí — es mucho más rápido de descartar.
  - **Código ya preparado (esqueleto, todavía no conectado a ninguna ruta):** migración `012_add_recall_bot_id.sql` (columna `recall_bot_id` en `meetings`), variables de entorno documentadas en `.env.example` (`RECALL_API_KEY`, `RECALL_REGION`, `RECALL_GOOGLE_LOGIN_GROUP_ID`), y `src/recall.ts` con `createRecallBot()` / `getRecallRecordingUrl()`.
  - **Pendiente para cerrar la fase:** (1) ~~confirmar que el bot entra a la reunión~~ **confirmado** — bot "Peitho" entró a una reunión de prueba real signed-in, sin sala de espera; (2) ~~confirmar el campo de la URL de descarga~~ **confirmado, con un fix real encontrado:** la respuesta del bot que completó la grabación (`bot_id: 2300017d-7b5a-4e35-aa39-464aedee4267`, ojo con este ID — es fácil transcribirlo mal a mano, mejor listar bots por `meeting_url` que confiar en copiar/pegar) trajo `media_shortcuts.audio_mixed: null` — Recall por default solo genera el video mezclado, no el audio-solo. Hay que pedirlo explícitamente con `recording_config: { audio_mixed_mp3: {} }` en el body de Create Bot — ya agregado a `createRecallBot()` en `recall.ts`. El path `recordings[0].media_shortcuts.audio_mixed.data.download_url` en `getRecallRecordingUrl()` queda confirmado correcto (ya no es placeholder), pendiente de probarlo end-to-end con un bot creado ya con este fix; (3) crear el endpoint de webhook (`POST /webhooks/recall`) que reciba el aviso de "grabación lista", baje el audio, y reutilice `analyzeMeetingAudio()` tal cual existe hoy; (4) ~~conectar la creación del bot al disparador (a) — match con el excel de metas~~ **hecho:** `scheduleRecallBotForMeeting()` en `recall.ts` — llamado desde `calendarSync.ts` (apenas se sincroniza el evento) y, de forma lazy/idempotente, desde `GET /meetings` (scope upcoming) y `GET /meetings/:id` (cubre el caso en que el excel se completa después de que la reunión ya se sincronizó). No agenda bot si: ya tiene `recall_bot_id`, la reunión ya pasó, o no hace match con el excel (`client_id` sigue null). **Disparador (b) — invitación manual del bot a una reunión — hecho (código), con pasos operativos pendientes:**
  - **Por qué hacía falta repensarlo:** se descubrió (conversación real con el usuario) que las reuniones reales con los 14 clientes de BullsEye **no** viven en el calendario de ningún ejecutivo de BullsEye — las agenda un SDR directo en el calendario del **ejecutivo comercial del cliente** (ej. alguien de CCHC, en el Workspace de Google/Microsoft de CCHC, no el nuestro), y el SDR **no** queda invitado al evento. Además, no todos los clientes usan Google Meet — de 17 clientes en la tabla de "cómo agenda cada uno": 3 con Meet directo, 4 con Microsoft Teams, y 9 con "enlace propio de HubSpot" (que HubSpot resuelve a Meet/Teams/Zoom según lo que tenga conectado ese cliente — no agrega una tercera plataforma real). Conclusión: el disparador (a) de match-con-excel **solo cubre lo que ya vive en un calendario que Peitho sincroniza** (hoy, prácticamente nada de los 14 clientes) — el disparador (b) es en la práctica el mecanismo principal, no un caso de borde.
  - **Cómo funciona:** el mismo mecanismo `/auth/google` + `POST /calendar/watch` que ya existe para los ejecutivos (sin cambios de código) se usa para conectar el calendario propio de `bot@peithob2b.com` (env var `PEITHO_BOT_GOOGLE_ACCOUNT_EMAIL`). Cuando alguien invita a `bot@peithob2b.com` como asistente de un evento (Meet o Teams — un correo de invitación .ics a una cuenta de Gmail normalmente se agrega solo al Google Calendar de esa cuenta, sin importar la plataforma del link), `calendarSync.ts` detecta que ese canal es el del bot (no el de un ejecutivo) y lo procesa con una rama nueva: `upsertMeetingFromBotInvite()` + `extractMeetingUrl()` (Meet vía hangoutLink/conferenceData, o Teams vía regex sobre location/description — no hay conferenceData estructurado para links ajenos a Google) + `extractContraparteFromBotInvite()` (a diferencia del caso normal, ni el organizador del evento — el ejecutivo comercial del cliente — ni el bot mismo son el prospecto; se excluyen ambos para encontrar al prospecto real). `ejecutivo` queda `null` a propósito (no hay ningún BullsEye en la llamada). Nueva columna `meeting_url` (migración `013_add_meeting_url_to_meetings.sql`) — a diferencia de `meet_code`, guarda la URL completa tal cual (necesario para Teams). `scheduleRecallBotForMeeting()` ahora acepta `{requireClientMatch: false}` para este disparador: a diferencia del disparador (a), una invitación manual agenda el bot **sin** esperar el match del excel (el match se sigue intentando igual, solo para clasificar a qué cliente pertenece en el dashboard). `createRecallBot()` solo agrega el campo `google_meet` si la URL es de `meet.google.com` — para Teams no hace falta nada por bot creado (ver abajo).
  - **Paso 1 — confirmado end-to-end (04-09-2026).** `bot@peithob2b.com` conectado vía `/auth/google` + watch registrado con `POST /calendar/watch`. Invitación manual real de prueba (Meet, cuenta personal de Gmail del usuario como contraparte, sin match en el excel a propósito) recorrió todo el flujo sin intervención: `[sync]` detectó el evento en el calendario del bot → `upsertMeetingFromBotInvite` guardó la reunión con `meeting_url` → `scheduleRecallBotForMeeting({requireClientMatch:false})` creó el bot en Recall pese a no matchear el excel → `bot agendado (...)`. Confirma que el disparador (b) funciona solo, sin ninguna intervención manual más allá de invitar al bot al evento.
    - **Tres bugs reales encontrados y corregidos en el camino** (todos con timeouts faltantes o env vars nunca cargadas — no problemas de diseño):
      1. `metasSheet.ts` (llamadas a Sheets API) y `recall.ts` (fetch a Recall) no tenían ningún timeout — una llamada colgada dejaba la promesa pendiente para siempre. Se agregó `timeout`/`AbortController` a ambos, mismo patrón que ya usaba `calendarSync.ts`.
      2. El pool de Postgres (`db.ts`) tenía `connectionTimeoutMillis` (solo cubre conectarse) pero no `query_timeout` — una consulta que ya está corriendo y se cuelga a mitad de camino podía quedar pegada para siempre. Se agregó `query_timeout: 15_000`.
      3. **El `.env` real del backend nunca tuvo `RECALL_API_KEY`/`RECALL_REGION`/`RECALL_GOOGLE_LOGIN_GROUP_ID`** — se habían usado únicamente en comandos `curl` sueltos contra la API de Recall, nunca dentro del propio backend. `scheduleRecallBotForMeeting` salía en silencio (sin loguear nada) apenas detectaba que Recall no estaba configurado, lo que parecía exactamente un colgue indefinido y llevó a perseguir los dos bugs de arriba antes de encontrar la causa real. **Aprendizaje:** agregar logs también en las salidas tempranas "no-op" de una función, no solo en errores — "terminó sin avisar" y "está colgado" son indistinguibles sin eso.
    - **Webhook `POST /webhooks/recall` confirmado end-to-end (04-09-2026, más tarde la misma noche).** Después de una sesión larga de debugging con varios 401 fantasma, se encontró el bug real leyendo el código fuente de `node_modules/svix/src/webhook.ts`: **`Webhook.verify()` en esta versión de la librería no devuelve el payload parseado — su tipo de retorno es literalmente `undefined`, solo lanza una excepción si la firma es inválida.** Como el código asumía que devolvía el JSON, CUALQUIER webhook (firma válida o no) terminaba con `payload = undefined` → 401 — nunca hubo un problema real de firma ni de headers, por eso "firma inválida" nunca apareció en los logs pese a horas de pruebas con "Replay" en el dashboard de Recall. Fix: tras `wh.verify()` (que no lanzó = firma OK), parsear el body nosotros mismos con `JSON.parse(body.toString('utf8'))`. Dos bugs más reales encontrados en el camino, ambos ya corregidos: (a) el naming de los headers de firma es `webhook-id`/`webhook-signature`/`webhook-timestamp` (estándar "Standard Webhooks"), no `svix-id`/etc. como asumía el código original — confirmado viendo un intento fallido real en el dashboard de Recall; (b) `clientsRouter.use(requireAuth)` sin el prefijo `/clients` interceptaba CUALQUIER request que llegara sin haber sido respondida por un router anterior (incluido `/webhooks/recall`, registrado después en `app.ts`) con 401 "Falta el token de sesión" — bug real de Express (un router montado sin path propio vía `app.use(router)` recibe TODO lo que no haya sido respondido por un router previo). **Confirmado real con un `bot.done` real vía Replay del dashboard de Recall:** `200 OK`, seguido de la secuencia completa automática — `[webhooks/recall] bot ...: descargando audio...` → `audio guardado en .../uploads/...` → `[analysis] ...: transcribiendo con Deepgram...` → `corriendo el prompt de análisis con Claude...` — sin ninguna intervención manual. **La Fase H queda funcionando de punta a punta para Google Meet: bot se agenda solo (match de excel o invitación manual) → entra a la reunión → graba → el webhook baja el audio → se transcribe y analiza automático, mismo pipeline de siempre.**
    - **Deepgram reemplazado por el transcript nativo de Recall (código listo, pendiente de probar con una reunión real).** El usuario notó que ya no tiene sentido bajar el audio y mandarlo a Deepgram nosotros, ya que Recall trae su propia transcripción con el **nombre real de cada hablante** (diarización por participante de la plataforma, no acústica) — resuelve de raíz la limitación conocida del heurístico "el primer hablante es el ejecutivo" (ver más abajo en este archivo, sección de aprendizajes de la Tarea 5). Implementado: `createRecallBot()` ahora pide también `recording_config.transcript.provider.deepgram_async` (formato de respuesta confirmado por documentación de Recall: array de segmentos `{ participant: { name }, words: [{ text }, ...] }`); nueva función `getRecallTranscriptUrl()` en `recall.ts`; `webhooks.ts` baja ese transcript además del audio y lo arma como texto con `buildTranscriptFromRecall()` (nueva, en `postMeetingAnalysis.ts`), guardándolo en la nueva columna `meetings.transcript_text` (migración `014_add_transcript_text_to_meetings.sql`); `analyzeMeetingAudio()` usa `transcript_text` directo si existe y **se salta Deepgram por completo** — Deepgram queda solo como fallback para la extensión de Chrome (que nunca tiene `transcript_text`). El `audio_mixed_mp3` se sigue pidiendo igual como respaldo (por si el transcript de Recall fallara), y si baja el transcript falla no rompe todo el flujo (queda `transcript_text` null y cae a Deepgram sobre el audio ya descargado). **No se hizo el nombre real → rol (ejecutivo/contraparte) a propósito:** el prompt de análisis ya recibe por separado los nombres de ejecutivo/contraparte (columnas de `meetings`), así que Claude puede correlacionarlos él mismo contra los nombres reales del transcript, sin necesidad de adivinar acá. **Pendiente:** confirmar con una reunión real que el schema del transcript es tal cual documentado (no se ha visto una respuesta real todavía, a diferencia de `audio_mixed` que sí se confirmó en producción) — si el nombre del campo no calza, `buildTranscriptFromRecall` devolvería texto vacío y caería en el error "No se encontró transcripción" en vez de fallar silenciosamente.
    - Pendiente de probar igual con una reunión de Teams una vez esté listo el punto 2.
  - **Pendiente — pasos operativos para Teams (Crossnet, Grupo Tress, DSAC, Vigatec — los clientes más críticos del usuario):** a diferencia de Google Meet, **no hace falta registrar nada en Azure AD** — Recall recomienda crear un solo tenant/organización nueva de Microsoft 365 para el bot (mismo patrón que `peithob2b.com` del lado Google), configurar el signed-in bot de Teams una vez a nivel de cuenta en el dashboard de Recall (sin config adicional por bot creado, a diferencia de `google_login_group_id`), y pedirle a cada uno de esos 4 clientes que agregue el dominio del bot como "organización de confianza" en la configuración de seguridad de Teams de su propio tenant (un ajuste único de su lado, no del nuestro). Fuente: [Configuring Microsoft Teams Signed-in Bots](https://docs.recall.ai/docs/setting-up-signed-in-bots-for-microsoft-teams), [Before Implementing Microsoft Teams Signed-in Bots](https://docs.recall.ai/docs/signed-in-teams-bots-overview). Confirmar después con una invitación real de Teams que el flujo completo funciona igual que Meet.
  - **Estado exacto donde quedamos (03-09-2026, a mitad del paso 1 de arriba) — retomar mañana desde acá:**
    - Ya hechos: `.env` local tiene `PEITHO_BOT_GOOGLE_ACCOUNT_EMAIL=bot@peithob2b.com` agregado; `GOOGLE_REDIRECT_URI` y `PUBLIC_BASE_URL` actualizados a la URL de ngrok de esa sesión (`https://reverend-motto-amenity.ngrok-free.dev` — **ojo, las URLs de ngrok gratis cambian cada vez que se reinicia**, si ya no es esa hay que repetir: actualizar `.env`, agregar la nueva URL + `/auth/google/callback` en Google Cloud Console → APIs & Services → Credentials → el OAuth Client → Authorized redirect URIs); esa misma URL de callback ya está agregada en Credentials; `bot@peithob2b.com` ya está agregado como **test user** en Google Auth Platform → Público (Audience) → Test users (necesario porque la app OAuth sigue en modo "Prueba", no publicada — límite 100 usuarios de prueba).
    - **Bloqueado justo antes del último paso** (visitar `/auth/google` con `bot@peithob2b.com` en una ventana de incógnito): el proyecto de Supabase **"Peitho AI" está pausado** (plan free, se pausa solo por inactividad) y el botón "Resume project" tira `Failed to restore project: This operation is currently unavailable. Partial System Outage` — confirmado con [status.supabase.com](https://status.supabase.com/) que Supabase tiene una falla real de Dashboard/API en curso, no es un problema nuestro. Se puede reintentar "Resume project" hasta el **07 Oct 2027** (fecha límite que muestra el aviso de pausa) antes de que el proyecto deje de ser reactivable.
    - **Para retomar:** (1) entrar a supabase.com → proyecto "Peitho AI" → click "Resume project" (probar de nuevo, puede que ya esté resuelta la falla); (2) una vez reactivado, confirmar que el backend local conecta bien (`npm run dev`, sin el error `ENOTFOUND tenant/user ... not found`); (3) confirmar que ngrok + `.env` siguen con la misma URL (si se reinició la Mac o se cerró ngrok, la URL cambia — repetir la actualización de `.env` + Google Cloud Console); (4) en incógnito, `https://TU-URL-NGROK.ngrok-free.dev/auth/google`, iniciar sesión con `bot@peithob2b.com`, aceptar permisos; (5) con el mensaje de "Cuenta de Google conectada: bot@peithob2b.com" confirmado, registrar el watch: `curl -X POST https://TU-URL-NGROK.ngrok-free.dev/calendar/watch -H "Content-Type: application/json" -d '{"google_account_email": "bot@peithob2b.com"}'`; (6) probar con una invitación real de Meet a `bot@peithob2b.com` para confirmar que aparece en `meetings` con `meeting_url` poblado y se agenda el bot de Recall.
    - Después de esto (funcionando Meet), seguir con el punto 2 (Teams: crear tenant de Microsoft + bot signed-in en Recall + pedir a Crossnet/Grupo Tress/DSAC/Vigatec que agreguen el dominio como organización de confianza). ~~agregar un selector admin-only para corregir manualmente el cliente de una reunión que no hizo match automático~~ **hecho:** `PUT /meetings/:id/client` (admin-only, valida que el cliente exista) + `AssignClientForm` en `/reuniones/[id]` (selector con los clientes existentes, visible solo para admin, junto al formulario de LinkedIn en "Ficha del contacto"); (5) por seguridad — la API key real de Recall se compartió sin encriptar varias veces en este chat (capturas y texto plano); el usuario decidió explícitamente rotarla recién cuando las pruebas funcionaran — **ya funcionaron, así que corresponde rotarla ahora**.

---

## Identidad de marca

Definida por el usuario (no vía el prompt de `docs/prompt_logo_peitho.md` que se le redactó — terminó decidiendo distinto a la recomendación ahí):
- **Logo/isotipo:** perfil de mujer griega con corona de laurel (referencia a Peitho, diosa de la persuasión), estilo lineal.
- **Colores corporativos: los mismos de BullsEye** (`#251762` morado oscuro, `#62E0D8` turquesa) — a diferencia de la opción de identidad 100% independiente que se le había sugerido, el usuario optó por que Peitho comparta paleta con BullsEye.
- **Tagline:** "Prepare - Perform - Close".
- El usuario está construyendo una **landing/web de marketing para Peitho** con Claude Design (no confundir con `peitho-frontend/`, que es la app interna de uso operativo) — trabajo en curso, fuera del alcance de este backend.

## Ideas para v2 (evaluar después, no ahora)

- **Bot que se une a la reunión como participante visible** (como DIIO/Gong), en vez de la extensión de Chrome. El usuario confirmó explícitamente que **no le importa** que se vea un bot en la llamada — la restricción "sin bot visible" del brief original ya no aplica como decisión de producto. Reemplazaría la Tarea 4 por completo: un servicio backend con Playwright que entra al link de Meet según el `start_time` de `meetings` (no según que el ejecutivo abra una pestaña), capturando audio vía WebRTC/dispositivo de audio virtual en vez de `chrome.tabCapture`. Es un desarrollo grande (varias sesiones), no una extensión de la Tarea 4 — evaluar viabilidad técnica antes de comprometerse. Decisión explícita: terminar primero la extensión de Chrome (Tarea 4) y avanzar con las Tareas 5-8; esto se evalúa después.

## Decisiones y aprendizajes (no repetir)

- El backend vive en `peitho-backend/` dentro de este mismo repo (`final-bullseye-abm-platform`), no en un repo separado — así no se gestiona infraestructura de git adicional.
- **Conexión a Supabase:** la connection string de "Direct connection" (`db.<ref>.supabase.co:5432`) falla con `ENOTFOUND` en la red del usuario porque ese hostname solo resuelve por IPv6. Usar siempre el **Session pooler** (`aws-0-<region>.pooler.supabase.com:5432`, usuario `postgres.<project-ref>`) en `DATABASE_URL`.
- Migraciones SQL simples en `migrations/*.sql`, corridas con `npm run migrate` (runner propio en `src/migrate.ts`, sin dependencias externas como Prisma/Knex — no se justifican para el tamaño actual del proyecto).
- La tabla `meetings` incluye únicamente los campos descritos en la sección 0 de `peitho_arquitectura_tecnica_v1.md` más metadata estándar (`id`, `created_at`, `updated_at`). Columnas de tareas futuras (ej. `analysis` de la Tarea 5) se agregan cuando corresponda, no antes.
- El proyecto Supabase del usuario se llama **"peitho"** (ya existía antes de empezar Tarea 1).
- **Google Calendar push notifications (`events.watch`) exigen una URL de webhook HTTPS pública** — no funciona con `localhost`. Para probar en local se usa ngrok (`ngrok http 3001`), seteando `GOOGLE_REDIRECT_URI` y `PUBLIC_BASE_URL` con esa URL. Riesgo conocido: Google puede exigir verificar el dominio del webhook en Search Console antes de aceptar el watch — si eso bloquea al usuario, evaluar probar directo en el deploy final (Railway/Render) en vez de forzarlo en local.
- `google.ts` valida las env vars de Google **dentro de las funciones**, no a nivel de módulo — si se valida al importar, el server entero no arranca (rompe `/health`) cuando el usuario todavía no configuró Google. Mantener este patrón para cualquier integración nueva que sea opcional/incremental.
- El scope de OAuth pedido es solo `calendar.readonly` (+ `userinfo.email`). La Tarea 8 (`events.patch` para insertar el link del brief) va a necesitar re-autorizar con un scope de escritura (`calendar.events`) — no está cubierto todavía.
- **La sincronización inicial de `events.list` necesita `timeMin` Y `timeMax`.** Sin `timeMax`, un evento recurrente sin fecha de fin hace que `singleEvents:true` devuelva cada ocurrencia futura sin parar (se manifestó como un "colgue" de varios minutos sin ningún error — en realidad estaba paginando cientos de eventos). Se acotó a una ventana de 90 días. Las sincronizaciones incrementales posteriores (con `syncToken`) no llevan estos filtros de tiempo.
- Todas las llamadas a la API de Google (`events.watch`, `events.list`) y al pool de Postgres (`connectionTimeoutMillis`) tienen timeouts explícitos + logs de progreso (`console.log('[watch] ...')` / `'[sync] ...'`) — sin esto, un problema de red se manifiesta como un colgue silencioso e indiagnosticable. Mantener este patrón (timeout + log de progreso) en las integraciones futuras que hagan llamadas de red externas (Deepgram, Anthropic API).
- Quedaron ~6 canales de `calendar_watch_channels` activos de las pruebas repetidas de esta tarea (cada llamada a `/calendar/watch` crea uno nuevo, no reemplaza el anterior) — todos apuntan al mismo webhook, así que un mismo evento se procesa varias veces en paralelo (no rompe nada por el `on conflict` en `meetings`, pero es redundante). Se limpiaron con `src/scripts/stopStaleChannels.ts` (dado de baja con `channels.stop` + borrado de la fila) — correr ese script si se vuelve a acumular.
- **La extensión de Chrome (`peitho-chrome-extension/`) apunta a `http://localhost:3001` directo, sin ngrok** — corre en el mismo Mac que el backend, así que no necesita el túnel público (eso solo es necesario para que Google *entre* al webhook desde internet, no para que la extensión *salga* hacia localhost).
- **`chrome.tabCapture` no se puede grabar desde el service worker** (no tiene acceso a `MediaRecorder`/`getUserMedia`, no tiene DOM). El patrón MV3 correcto es: el service worker consigue el `streamId` con `chrome.tabCapture.getMediaStreamId()`, y un **offscreen document** (`chrome.offscreen.createDocument`, `offscreen.html`/`offscreen.js`) hace la grabación real y sube el archivo.
- El audio se guarda en `peitho-backend/uploads/` (disco local, gitignored) — funciona para probar en local, pero **no sirve para el deploy final** (Railway/Render suelen tener filesystem efímero). Antes de desplegar, evaluar mover esto a Supabase Storage o S3.
- **`chrome.tabCapture` no se puede activar 100% en automático desde el background — Chrome exige un gesto del usuario** (ej. clic en el ícono de la extensión) para autorizarlo, es una restricción de seguridad de la plataforma, no algo evitable con código (se confirmó probando: error real "Extension has not been invoked for the current page (see activeTab permission)"). El diseño quedó así: detección automática (ícono se pone celeste) + un clic del ejecutivo para arrancar la grabación (ícono se pone rojo) vía `chrome.action.onClicked`. Esto no rompe el requisito de "sin bot visible en la llamada" (el clic es local, nadie más lo ve), pero sí significa que no es 100% zero-touch.
- **Modelo de Claude usado: `claude-sonnet-5`** (el actual, reemplaza al `claude-sonnet-4-6` que sugería el brief original — mismo precio, mejor calidad). Con `thinking: {type: 'disabled'}` explícito — es una tarea de extracción/evaluación de un solo paso sobre una transcripción ya dada, no necesita razonamiento extendido, y evita el riesgo de que `max_tokens` se consuma entre thinking + la respuesta JSON (en Sonnet 5, si no se especifica `thinking`, corre adaptive por defecto y comparte el mismo `max_tokens`).
- El prompt de análisis se separó en dos partes para aprovechar **prompt caching**: la parte fija (instrucciones + esquema, `ANALISIS_SYSTEM_PROMPT`) va como `system` con `cache_control: ephemeral`, y el contexto variable de cada reunión (empresa, ejecutivo, contraparte, transcripción) va en el mensaje de usuario. Así solo se paga precio completo de esa parte fija una vez por ventana de caché, no en cada reunión.
- `EMPRESA_CLIENTE` ("BullsEye") y `PLAYBOOK` ("Ventas") están **hardcodeados** en `postMeetingAnalysis.ts` — Peitho todavía no modela múltiples clientes/empresas (eso es una idea de la plataforma BullsEye ABM, no de Peitho en su MVP actual). Si Peitho se usa para más de una empresa cliente, esto hay que revisarlo.
- Sin diarización por rol, Deepgram solo distingue "hablante 0, hablante 1, ..." por voz — se asume que el primer hablante que aparece cronológicamente es el ejecutivo (quien inicia la llamada), tal como sugiere la arquitectura del prompt. Con 3+ hablantes distintos, todos los que no son el primero se etiquetan igual como "Contraparte" (simplificación del MVP).
- **Bug crítico detectado probando la Tarea 5 con una segunda persona real ("Jaime") en la llamada: `chrome.tabCapture` capturaba solo el audio entrante de la pestaña, nunca el micrófono, y además dejaba al ejecutivo sin poder escuchar al otro participante mientras grababa.** Causa raíz (confirmada investigando el sample oficial de Google, `functional-samples/sample.tabcapture-recorder`): capturar el stream de audio de una pestaña vía `getUserMedia({chromeMediaSource:'tab'})` lo desvía de los parlantes normales — hay que reconectarlo explícitamente. Esto también significaba que la voz del propio ejecutivo nunca se grababa, solo lo que él escuchaba. Fix implementado en `offscreen.js`: se capturan ambos streams (tab + mic) y se mezclan con la Web Audio API (`AudioContext.createMediaStreamDestination()`), y el stream de la pestaña se reconecta también a `audioContext.destination` para que el ejecutivo lo siga escuchando con normalidad. Complicación adicional: **un offscreen document no puede mostrar el diálogo de permiso de micrófono de Chrome** (no tiene UI visible) — `getUserMedia({audio:true})` falla directo con `NotAllowedError` la primera vez. Se resolvió con una página de configuración de una sola vez (`mic-permission.html`/`.js`, abierta como pestaña normal vía `chrome.tabs.create`, gatillada después del primer clic en el ícono si `chrome.storage.local` no tiene `micPermissionGranted`) — el permiso concedido ahí queda guardado por origen (`chrome-extension://<id>`) y el offscreen document (mismo origen) puede usarlo después sin volver a preguntar. Si el usuario nunca hace esa configuración, la extensión sigue grabando solo el audio entrante (degradación aceptable, no bloquea nada) — se loguea un `console.warn` con la URL exacta a abrir.
- **Confirmado con la prueba real de dos personas:** el fix de audio (mic + reconexión a parlantes) funcionó — el ejecutivo escuchó a la otra persona con normalidad durante toda la grabación. Se descartó por código (revisando `routes/meetings.ts` y `postMeetingAnalysis.ts`) la hipótesis de que un análisis viejo quedara "pegado" al reusar el mismo link de Meet: cada subida de audio genera un archivo con nombre único (`${id}-${Date.now()}.webm`), sobreescribe `audio_path`, y `analyzeMeetingAudio` siempre transcribe ese archivo nuevo y sobreescribe `analysis` — no hay caché ni reuso de texto anterior. Lo que sí se observó en esa prueba: Deepgram detectó un solo hablante en vez de dos (todo salió etiquetado "Ejecutivo" por el heurístico de "primer hablante"), probablemente por ser una llamada corta y/o mezcla de audio en un solo canal — limitación de diarización ya documentada más arriba, no relacionada con el bug de audio. El usuario decidió explícitamente dar la Tarea 5 por cerrada sin investigar esto más a fondo por ahora (aceptado como limitación conocida del MVP).
- **`peitho-frontend/` es una app Next.js 14 separada** (no vive dentro de `bullseye-abm-platform`), consume la API de `peitho-backend` vía fetch server-side en Server Components (`GET /meetings?scope=upcoming|past`, nuevo endpoint en `routes/meetings.ts`) — como el fetch corre en el servidor de Next.js, no en el navegador, no hace falta configurar CORS en `peitho-backend`. Auth: mismo patrón que `bullseye-abm-platform` (Supabase Auth + `middleware.ts`), pero apunta al proyecto Supabase **"peitho"**, no al de BullsEye ABM — son usuarios y credenciales completamente separados, hay que crear el usuario de nuevo en Supabase Studio del proyecto "peitho" aunque ya exista en el otro proyecto. Corre en el puerto `3002` (`peitho-backend` ya usa `3001`).
- Se fijó `next` en `14.2.35` (no `14.2.15` como sugería el brief original) — es el último parche de la rama 14.2.x disponible al momento de crear el proyecto, corrige varias vulnerabilidades conocidas de esa versión. Saltar a Next 16 (mayor, breaking) no se evaluó — mantener la misma versión mayor que `bullseye-abm-platform` para consistencia del stack.
- **La página de detalle de reunión vive en una ruta compartida `/reuniones/[id]`** (no `/reuniones/pasadas/[id]` ni separada por módulo) — una reunión puede tener `analysis` aunque su `start_time` todavía no haya pasado (ej. agendada para más tarde el mismo día pero ya grabada como prueba), así que quedaba en la lista de "futuras" sin poder verse su detalle si la ruta solo estaba enlazada desde "pasadas". Ambas tablas (`MeetingsTable`, con prop `detailBasePath`) enlazan a esta misma ruta; la página maneja el caso de que todavía no haya análisis mostrando un mensaje en vez de fallar.
- **`GET /meetings` excluye reuniones internas** comparando `empresa_contraparte` contra el dominio propio (`bullseye-abm.com`, constante `INTERNAL_DOMAIN` en `routes/meetings.ts`) — mismo patrón de hardcoding que `EMPRESA_CLIENTE`. A Peitho solo le interesan reuniones con prospectos reales, no reuniones entre miembros del equipo de BullsEye. No borra filas de la base, solo las excluye de la respuesta del endpoint.
- **El research pre-reunión usa la herramienta server-side `web_search` de Claude** (`claude-sonnet-5` con `tools: [{type:'web_search_20260209', name:'web_search', max_uses:5}]`) en vez de integrar Perplexity/Lusha/un scraper de LinkedIn dedicado — el modelo busca por su cuenta y el `max_uses` acota el costo. Con esta herramienta, la respuesta trae varios bloques de contenido intercalados (texto + resultados de búsqueda); el JSON final hay que leerlo del **último** bloque de tipo texto, no de `content[0]` como en el análisis post-reunión (que no usa tools). `thinking: {type:'disabled'}` sigue funcionando igual con la tool activada en Sonnet 5 (esa restricción de "disabled solo hasta effort high" es específica de Opus 5, no aplica acá).
- **Bug real detectado con una reunión real: el research decía "no se encontró información" a pesar de que el dato (LinkedIn del contacto) se encuentra a mano en Google en segundos.** Se agregó un log por búsqueda (`query` + cantidad de resultados, en `preMeetingBrief.ts`) para diagnosticarlo, y el log reveló la causa real: las primeras 5 búsquedas (dentro del `max_uses:5`) sí trajeron resultados buenos (7-10 resultados cada una, incluyendo la búsqueda exacta que encontró el LinkedIn), pero el modelo seguía intentando buscar más allá del límite (errores `max_uses_exceeded`, desperdiciados) y terminaba redactando "no fue posible encontrar información", ignorando sus propios resultados buenos anteriores. Fix: nueva instrucción explícita en el prompt (`PRE_REUNION_SYSTEM_PROMPT`, punto 4) — al llegar al límite de búsquedas, detenerse de inmediato y redactar el brief con lo ya encontrado, revisando los resultados de cada búsqueda anterior antes de concluir que la información es insuficiente. **Confirmado por el usuario** con una prueba real donde el modelo hizo 5 búsquedas limpias sin reintentos desperdiciados y el brief final salió rico y preciso.
- **Bug real: el botón "Iniciar research" quedaba deshabilitado para siempre ("Investigando...") si el proceso del backend moría a mitad de una research** (ej. `Ctrl+C`, o un crash) — el fire-and-forget nunca llegaba a su `catch`, así que `pre_brief_status` se quedaba en `'running'` sin ninguna forma de reintentarlo desde el frontend. Fix: al arrancar el servidor (`server.ts`), cualquier fila con `pre_brief_status='running'` se resetea a `'failed'` — es necesariamente una research vieja de una corrida anterior, no puede haber ningún proceso en memoria todavía trabajando en eso justo al iniciar.
- **El botón "Iniciar research" es un client component que llama a rutas proxy de Next.js** (`app/api/meetings/[id]/research`, `app/api/meetings/[id]`), no directo a `peitho-backend` — a diferencia de las páginas de listado/detalle (Server Components, fetch server-side), un botón con `onClick` corre en el navegador, y sin este proxy chocaría con CORS (no configurado en el backend). El componente hace polling liviano (cada 4s) a la ruta proxy para saber cuándo `pre_brief_status` deja de ser `'running'`, y ahí llama `router.refresh()` para traer el resultado ya guardado.
- **El timeout del research pre-reunión (llamada a Claude con la tool `web_search`) es de 3 minutos** (`{ timeout: 180_000 }` en `preMeetingBrief.ts`), no 90s como al principio — con `web_search` cada búsqueda implica una llamada de red + razonamiento adicional del modelo, así que 90s tiraba `APIConnectionTimeoutError` siempre (confirmado probando en vivo). Si en el futuro se agregan más `max_uses` o herramientas adicionales, puede que este timeout necesite subir más.
- **El excel de metas (fuente de nombre/cargo/industria/cliente del contacto) es el Google Sheet "Metas equipo BullsEye / reuniones / oportunidades"**, tab **"API Reuniones - IA"** (columnas `Cliente | ID Cliente | Empresa | Contacto | Cargo | Correo | Industria | Fecha de la reunión | ID Reunión`, ~1930 filas). `metasSheet.ts` no hardcodea el nombre del tab — lo detecta buscando cuál de todos los tabs del spreadsheet (tiene ~15-20, para comisiones/prospección/OKRs/etc., no relacionados) tiene esas columnas exactas, así que sobrevive si alguien lo renombra. El matching de una reunión de `meetings` contra una fila del excel es por empresa (normalizada, case-insensitive) + fecha más cercana como desempate — es una hoja mantenida a mano con typos reales (fechas invertidas, nombres de empresa con variaciones), así que es best-effort: si no hay match confiable, los campos quedan `null` en vez de forzar un dato incorrecto.
- **La lectura del excel de metas reutiliza el mismo OAuth de Google que ya existía para Calendar**, no una cuenta de servicio nueva — se le agregó el scope `spreadsheets.readonly` a `GOOGLE_SCOPES` (antes `CALENDAR_SCOPES`) en `google.ts`. Cualquier cuenta ya conectada antes de este cambio no tiene ese scope todavía — hay que volver a pasar por `/auth/google` con esa cuenta para que quede autorizada (Google no lo pide solo, hay que re-consentir). También hubo que habilitar la Google Sheets API y agregar ese scope en la pantalla de consentimiento de OAuth del proyecto en Google Cloud Console (no basta con el código, si el scope no está declarado ahí el consentimiento falla).
- El excel de metas ya trae **"BullsEye" como un valor más de la columna Cliente** (la agencia también prospecta para sí misma) — la tabla `clients` no necesita ningún caso especial para esto, el find-or-create por nombre lo crea igual que a cualquier otro cliente real.
- **La tool `web_fetch` de Claude (`web_fetch_20260209`) no puede traer contenido de LinkedIn** — devuelve `web_fetch_tool_result_error` con `error_code: "url_not_allowed"` sin importar cuántas veces se reintente, es un bloqueo a nivel de política de la herramienta (no de indexación ni de robots.txt del sitio). Confirmado real intentando traer la URL de LinkedIn que el ejecutivo pegó a mano para el contacto "Felipe Almazan". No usar `web_fetch` para LinkedIn en ningún research futuro — si se necesita contenido de un perfil de LinkedIn, la única vía viable con las herramientas nativas de Claude es `web_search` (que sí indexa parcialmente contenido público de LinkedIn a través de Google/Bing).
- **Módulo 3 (Base de conocimiento) — varios bugs reales encontrados subiendo un PDF real de 92MB (el ICP de BullsEye):**
  - `officeparser` (extracción de texto de pdf/docx/pptx/etc.) trae por defecto una versión de `pdfjs-dist` con una vulnerabilidad alta conocida (ejecución de JS arbitrario al abrir un PDF malicioso) — se corrió `npm audit fix --force` (bajó a `officeparser@6.0.7`) antes de dejar esto expuesto a subidas de usuarios. Revisar esto de nuevo si se actualiza `officeparser`.
  - `createClient()` de `@supabase/supabase-js` inicializa su cliente de Realtime al crearse aunque solo se use Storage, y esa versión exige WebSocket nativo (recién disponible desde Node 22) — en Node 20 tiraba "Node.js detected but native WebSocket not found". Se pasa la librería `ws` explícitamente como `realtime.transport` al crear el cliente (`getSupabaseAdminClient()` en `knowledgeBase.ts`).
  - Supabase Storage rechaza rutas con espacios u otros caracteres fuera de `[a-zA-Z0-9.-_]` con "Invalid path specified in request URL" — se sanea el nombre del archivo solo para la ruta de storage (`sanitizeForStoragePath`), el nombre original se sigue mostrando tal cual.
  - **El límite de tamaño de subida es 50MB FIJO en el plan free de Supabase Storage**, no configurable desde el código ni desde la configuración del bucket (Supabase Studio → Storage → Settings lo confirma explícitamente: "Free Plan has a fixed upload file size limit of 50 MB"). El límite de `multer` en el backend se alineó a 50MB para que el error salga de inmediato y consistente, en vez de pasar nuestro límite y fallar después con un mensaje distinto de Supabase. Si el proyecto pasa a plan Pro, subir `MAX_FILE_SIZE_MB` en `routes/clients.ts`.
  - `req.file.originalname` de multer/busboy llega decodificado como latin1 (así llegan las cabeceras HTTP) — cualquier nombre de archivo con acentos o guiones especiales sale corrupto si no se re-decodifica como UTF-8 (`Buffer.from(originalname, 'latin1').toString('utf8')`, en `routes/clients.ts`).
  - `SUPABASE_URL` debe ser exactamente el "Project URL" de Project Settings → API (ej. `https://xxxx.supabase.co`, sin `/rest/v1/` ni `/` al final) — pegar la URL con esos sufijos de más rompe todas las llamadas a Storage con "Invalid path specified in request URL" (fácil de confundir con el bug de caracteres del nombre de archivo, son cosas distintas).
- **`meetings.recurring_event_id` distingue ocurrencias de una serie recurrente de Calendar** (migración `007_add_recurring_event_id_to_meetings.sql`) — Google marca cada ocurrencia expandida (`singleEvents:true`) con el id del evento "maestro" que la originó (`event.recurringEventId`, capturado en `calendarSync.ts`). `GET /meetings` excluye filas con este campo no nulo en ambos scopes: una reunión recurrente casi nunca es con un prospecto nuevo, y sin este filtro Google sigue generando ocurrencias futuras hacia adelante sin límite (se vieron filas reales hasta el año 2051 en la cuenta del usuario). **Confirmado funcionando end-to-end** — `src/scripts/backfillRecurringEventId.ts` completó retroactivamente 3817 de 5218 reuniones ya guardadas (el resto son reuniones genuinamente puntuales), y las listas del frontend quedaron limpias. Detalles del script: pide a Google la lista completa de eventos por ejecutivo con `events.list` (no una consulta por reunión — el primer intento con 7198 consultas individuales fue demasiado lento y una conexión de Postgres inactiva tumbó el proceso a mitad de camino, ver el fix en `db.ts` de un listener `pool.on('error', ...)` para que eso no vuelva a pasar), usando un **rango de fechas dinámico** calculado desde el min/max `start_time` de las reuniones pendientes de cada ejecutivo (un rango fijo de solo +3 años no llegaba a cubrir series ya sincronizadas hasta 2050-2051, lo que causó 0 coincidencias en el primer intento con el rango dinámico corregido — no fue, como se sospechó al principio, un problema de `calendarId`, que resultó ser `"primary"` correctamente en ambos intentos).

---

## Cómo correr y probar localmente

```bash
cd peitho-backend
cp .env.example .env   # completar DATABASE_URL con el Session pooler de Supabase
npm install
npm run migrate        # crea la tabla meetings
npm run dev             # levanta el servidor en :3001
curl http://localhost:3001/health
```

Respuesta esperada: `{"status":"ok","db":"connected","timestamp":"..."}`.

### Asignar el primer admin de Peitho (Fase E)

`/admin/usuarios` (la UI para asignar roles) requiere ya ser admin para entrar — es un huevo y la gallina la primera vez. El primer admin se inserta directo por SQL en **Supabase Studio → SQL Editor** (proyecto "peitho"), asumiendo que ese usuario ya existe en Authentication → Users:

```sql
insert into peitho_user_roles (user_id, email, role, client_id)
select id, email, 'admin', null
from auth.users
where email = 'tu-email@bullseye-abm.com'
on conflict (user_id) do update set role = 'admin', client_id = null;
```

Después de eso, desde `/admin/usuarios` en la app ya se pueden asignar el resto de los roles (admin o cliente) a cualquier usuario que ya exista en Supabase Auth.
